設計一個最大尺度是小時的任務流程進度管理倒數計時系統
1. 使用者可以新增任務節點，會有以下屬性：
    1. 任務名稱
    2. 任務可用時間，單位是分鐘
    3. 完成狀態

2. 任務節點樹狀的資料結構，每個任務結點下可以新增兩個子任務節點，並讓用使用根據是否成功完成父節點來決定接下來要去做的任務是哪一個子節點

3. 介面要是直向卷軸式，過去的任務在上面，接下來的下面，中間現在要做的任務會維持在螢幕的正中央，如img_0119.png所示。 過去的任務用毛玻璃特效表示。當任務可用時間到時，會閃紅色邊框表示，只有使用者輸入完成狀態後，才會到下一個任務。全域的倒數計時器設定在畫面的左邊，顯示剩餘時間。

4. 系統的總時間大約會介於1~2小時之間

使用流程：
1. 設定總時間長度
2. 建立任務樹，每個任務節點下可以新增兩個子任務節點，並讓用使用根據是否成功完成父節點來決定接下來要去做的任務是哪一個子節點
3. 按下開始鍵後開始計時

系統要求：
1. 做成chrome based的app，讓使用者可以透過chrome 下載安裝後離線使用
2. 如果可行，要有懸浮視窗的功能，使用者在其他視窗時也能看到計時器，並且可以調整大小和透明度
3. 可以儲存任務設定，讓使用者可以重複使用
